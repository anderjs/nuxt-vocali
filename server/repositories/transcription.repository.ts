import { VocaliTable } from "../utils/tables.js";

export class TranscriptionRepository {
  async listTranscriptions(): Promise<[]> {
    const table = VocaliTable.TRANSCRIPTIONS;
    void table;

    return [];
  }
}
